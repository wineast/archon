# 评审意见书：Eval Run/Batch 配置快照事务隔离——证据链完整，可合并

> 评审时间：2026-03-03 00:15
> 链路类型：缺陷链路
> 审查报告：DEFECT.md, FIX_REPORT.md, VERIFY_REPORT.md, TEST_GUARD_REPORT.md
> 分支：`dev-eval-run-config-snapshot-no-transaction-20260302`

## 0. 审查范围

| 报告 | 角色 | 状态 |
|------|------|------|
| DEFECT.md | 起诉书 | ✅ 已读取 |
| FIX_REPORT.md | 辩护词 | ✅ 已读取 |
| VERIFY_REPORT.md | 审判书 | ✅ 已读取 |
| TEST_GUARD_REPORT.md | 守护测试 | ✅ 已读取 |

## 1. Chain Completeness（链路完整性）

### 报告存在性
四份报告齐全（三份必需 + 一份可选守护报告）。

### 骨架完整性
| 报告 | 缺失要素 |
|------|----------|
| DEFECT.md | — |
| FIX_REPORT.md | — |
| VERIFY_REPORT.md | — |

- **DEFECT.md**：Delta（期望 vs 实际）、Reproduction Path（代码级竞态分析 + 5 次查询窗口图）、Location（三文件定位 + 双层根因）、Impact（严重度 + 范围）——四要素齐全
- **FIX_REPORT.md**：Root Cause（因果链 4 步）、Change（4 文件明细表）、Rationale（3 个替代方案 + 排除理由 + 已知局限）、Blast Radius（直接/间接/不影响三层）、Verification（静态+正向+回归）——五要素齐全
- **VERIFY_REPORT.md**：Reproduction（代码审计 4 点确认）、Coherence（三项检查 + 修正记录）、Boundary（4 变体表格）、Regression（静态+Blast Radius 区域验证表格）、Verdict（合并 + 摘要 + 残留风险）——五要素齐全

### 结果
✅ 完整。所有报告存在，核心要素无缺失。

## 2. Cross-Report Consistency（跨报告一致性）

### DEFECT → FIX 追溯

| DEFECT 条目 | 类型 | FIX 对应 | 结果 |
|------------|------|----------|------|
| 5 次独立查询无事务，并发修改导致不一致快照组合 | Delta→Root Cause | 5 次查询独立 await 无事务包裹，Read Committed 下各自独立快照 | ✅ 因果通——Root Cause 直接导出 Delta 描述的现象 |
| 代码级竞态，无法 UI 稳定复现；5 次查询窗口图 | Path→Verification | 代码审计确认事务包裹 + typecheck + 1390 测试 | ✅ 镜像——同为代码结构级验证 |
| `run/route.ts:56-106`, `batch/route.ts:60-110`, `resolve.ts:9-21` | Location→Change | `run/route.ts:10,57-153`, `batch/route.ts:10,62-186`, `resolve.ts:1-21` | ✅ 吻合——同文件同区域，行号扩大是因为增加了事务包裹和 ConfigError |
| 单用户低概率；多人协作/Batch 模式概率升高；自动化场景风险增大 | Impact→Blast Radius | 直接影响 run/batch 创建；间接影响无；不影响 Inngest worker、其他 eval API、resolve 其他调用方 | ✅ 覆盖——Impact 区域全部在 Blast Radius 中有对应声明 |

### FIX → VERIFY 追溯

| FIX 声明 | 类型 | VERIFY 对应 | 结果 |
|----------|------|-------------|------|
| Root Cause：独立查询无事务 | Root Cause→Coherence | Coherence 三项检查全做：因果可解释 Delta、Change 可消除 Root Cause（经修正）、Rationale 无漏洞 | ✅ |
| Blast Radius：run/batch 直接影响；resolve 其他调用方不影响；Inngest 不影响 | Blast Radius→Regression | Regression 表格逐项验证 5 个区域，均"正常" | ✅ |

### DEFECT → VERIFY 跨层追溯

| 检查项 | 结果 |
|--------|------|
| Reproduction 走的是原始 Path？ | ✅ — DEFECT 路径为代码审计（并发竞态无法 UI 复现），VERIFY Reproduction 同为独立代码审计，路径一致 |
| Delta 的期望行为已恢复？ | ✅ — DEFECT 期望"所有配置查询在同一事务中"，VERIFY 确认"全部在 `db.transaction({ isolationLevel: 'repeatable read' })` 内" |

### 发现的偏差
无。

**补充说明**：VERIFY_REPORT 记录了一个过程性修正——初始修复未指定隔离级别（默认 Read Committed），验证阶段发现后修正为 Repeatable Read。FIX_REPORT 反映的是修正后的最终状态。两份报告在描述同一事实时用词不同但指向一致——不构成矛盾，而是过程透明度的体现。

### 结果
✅ 一致。三份报告之间的链式传递无断裂，因果链、路径、位置、范围均可追溯。

## 3. Evidence Sufficiency（证据充分性）

### 验证报告证据审查

| 维度 | 有结论 | 有证据 | 证据支撑结论 |
|------|--------|--------|-------------|
| Reproduction | ✅ 通过 | ✅ 代码审计 4 点确认（行号级引用） | ✅ 此为代码结构级竞态，代码审计是恰当的验证方式 |
| Coherence | ✅ 一致（经修正） | ✅ PostgreSQL 文档引用 + 逻辑推导 + 修正记录 | ✅ 隔离级别修正有理论依据 |
| Boundary | ✅ 通过 | ✅ 4 变体表格，每项有条件/结果/理由 | ✅ 覆盖了向后兼容、错误处理、Inngest 时序、batch 循环 |
| Regression | ✅ 通过 | ✅ typecheck + 1390 测试 + 5 区域逐项验证表 | ✅ |

无截图资产——合理，因为代码结构级并发竞态没有可截图的 UI 状态。

### 代码抽查

| 文件 | 报告声称 | 实际代码 | 一致 |
|------|----------|----------|------|
| `resolve.ts:9-13,20-22` | 新增 `Tx` 类型导出 + `conn` 可选参数默认 `db` | `export type Tx = PgTransaction<...>`; `conn: Tx \| typeof db = db` | ✅ |
| `run/route.ts:61,147` | 配置查询+创建包入 `db.transaction({ isolationLevel: "repeatable read" })` | L61: `db.transaction(async (tx) => {`; L147: `}, { isolationLevel: "repeatable read" })` | ✅ |
| `batch/route.ts:68,180` | 同 run 模式，包括 batch 创建 + N 个 run 循环 | L68: `db.transaction(async (tx) => {`; L180: `}, { isolationLevel: "repeatable read" })`; 循环在 L142-177 | ✅ |

抽查额外确认：
- `run/route.ts:156` Inngest 事件在事务块**之后**发送 ✅
- `batch/route.ts:191` 同上 ✅
- 事务内所有 select/insert 均使用 `tx` 而非 `db` ✅

### 结果
✅ 充分。关键声明均有代码级证据，代码实际状态与报告描述一致。

## 4. Verdict Soundness（裁定合理性）

### 证据-结论一致性
四项检查结果：Reproduction 通过 + Coherence 一致（经修正）+ Boundary 通过 + Regression 通过 → Verdict "合并"。推导成立，属于"全通过 + 合并"的合理模式。

### 因果一致性特别审查
Root Cause → Change 是**治病**而非止痛：
- 不是"加延迟"（增大窗口期让竞态更难触发）
- 不是"加 try-catch"（吞掉不一致异常）
- 不是"加空值检查但不知 null 来源"
- **是**将独立查询包入 `db.transaction({ isolationLevel: "repeatable read" })`——这是 Postgres 并发读一致性的教科书解法，从机理上消除不一致窗口

验证阶段发现并修正隔离级别的过程尤其可信——说明验证者独立质疑了 Read Committed 下事务的实际语义，查阅了 PostgreSQL 官方文档，最终推动了从默认隔离级别到 Repeatable Read 的修正。这不是橡皮图章式验证。

### 遗漏风险
无。残留风险（`existingRunningBatch` TOCTOU）在 FIX_REPORT 和 VERIFY_REPORT 中均被明确标记为独立缺陷，不影响本次修复目标。

### 守护报告信号
TEST_GUARD_REPORT：19/19 测试全部通过，覆盖全部 Cause Anchor（11）、Boundary Set（6）、Blast Shield（2）。守护信号与 Verdict 一致。

### 结果
✅ 合理。Verdict 从证据自然推出，无逻辑跳跃。因果链修复方式是结构性根治而非止痛。

## 5. Decision（合并决策）

### 判决
✅ 可合并

### 四维度摘要
- **Completeness**：四份报告齐全，核心要素无缺失
- **Consistency**：三份报告链式传递无断裂，因果/路径/位置/范围均可追溯
- **Evidence**：代码抽查 3 处全部与报告声称一致，验证维度均有充分证据
- **Soundness**：全维度通过→合并，推导成立；修复方式为教科书级事务隔离，非止痛

### 理由
整条证据链完整可信：缺陷定位精准（代码级并发窗口分析）、修复对症（事务 + Repeatable Read）、验证过程中发现并修正了隔离级别遗漏（体现独立质证能力）、守护测试 19/19 通过锁定根因。四个审查维度均无异常。

### 阻塞项
无

### 建议
- `Tx` 类型目前在 `resolve.ts` 中定义。若后续更多模块需要事务支持，可考虑提取到 `@/db/types`（非阻塞，已在报告中记录）
