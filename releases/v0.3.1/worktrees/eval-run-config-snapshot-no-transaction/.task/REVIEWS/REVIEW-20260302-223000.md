# 评审意见书：Eval Run/Batch 配置快照事务隔离 — 证据链完整，可合并

> 评审时间：2026-03-02 22:30
> 链路类型：缺陷链路
> 审查报告：DEFECT.md, FIX_REPORT.md, VERIFY_REPORT.md, TEST_GUARD_REPORT.md
> 分支：`dev-eval-run-config-snapshot-no-transaction-20260302`

## 0. 审查范围

| 报告 | 角色 | 状态 |
|------|------|------|
| DEFECT.md | 起诉书 | ✅ 已读取 |
| FIX_REPORT.md | 辩护词 | ✅ 已读取 |
| VERIFY_REPORT.md | 审判书 | ✅ 已读取 |
| TEST_GUARD_REPORT.md | 守护测试 | ✅ 已读取 |

## 1. Chain Completeness（链路完整性）

### 报告存在性
四份报告齐全，含可选的守护报告。

### 骨架完整性
| 报告 | 缺失要素 |
|------|----------|
| DEFECT.md | — |
| FIX_REPORT.md | — |
| VERIFY_REPORT.md | — |

### 结果
✅ 完整
四份报告核心要素齐全，无缺失。

## 2. Cross-Report Consistency（跨报告一致性）

### DEFECT → FIX 追溯

| DEFECT 条目 | 类型 | FIX 对应 | 结果 |
|------------|------|----------|------|
| 5 次独立查询无事务 → 并发修改产生不一致快照 | Delta→Root Cause | 独立查询在 Read Committed 下各自拿执行时刻快照 | ✅ 因果通 |
| Q1-Q5 查询序列 + 窗口 A/B 分析 | Path→Verification | 代码审计确认全部查询在事务内 | ✅ 镜像 |
| `run/route.ts:56-106`、`batch/route.ts:60-110`、`resolve.ts:9-21` | Location→Change | `run/route.ts:10,57-153`、`batch/route.ts:10,62-186`、`resolve.ts:1-21` | ✅ 吻合 |
| eval run/batch 使用错误配置组合，评测结果不可信 | Impact→Blast Radius | 直接影响 eval/run 和 eval/batch 创建端点，不影响 Inngest worker 和其他 eval API | ✅ 覆盖 |

### FIX → VERIFY 追溯

| FIX 声明 | 类型 | VERIFY 对应 | 结果 |
|----------|------|-------------|------|
| 根因：独立查询无事务隔离 | Root Cause→Coherence | Coherence 确认因果链成立，且发现初始修复遗漏隔离级别，已修正为 Repeatable Read | ✅ |
| 直接影响 eval/run、eval/batch；不影响其他调用方和 Inngest worker | Blast Radius→Regression | Regression 逐项验证 5 个区域均无回归 | ✅ |

### DEFECT → VERIFY 跨层追溯

| 检查项 | 结果 |
|--------|------|
| Reproduction 走的是原始 Path？ | ✅ 同样是代码级查询序列审计，路径一致 |
| Delta 的期望行为已恢复？ | ✅ 全部查询 + 创建已在 `db.transaction({ isolationLevel: "repeatable read" })` 内 |

### 发现的偏差
无

### 结果
✅ 一致
三份报告链式传递无断裂，因果链 Delta→Root Cause→Change→Reproduction 完整闭合。

## 3. Evidence Sufficiency（证据充分性）

### 验证报告证据审查

| 维度 | 有结论 | 有证据 | 证据支撑结论 |
|------|--------|--------|-------------|
| Reproduction | ✅ | ✅ 代码审计指出具体行号 | ✅ |
| Coherence | ✅ | ✅ 引用 PostgreSQL 官方文档论证隔离级别 | ✅ |
| Boundary | ✅ | ✅ 4 个变体逐项验证 | ✅ |
| Regression | ✅ | ✅ typecheck + 1390 测试 + 5 区域逐项验证 | ✅ |

注：两个 assets 目录均为空。此缺陷为代码结构级并发竞态，截图无意义，代码审计是恰当的证据形式。

### 代码抽查
| 文件 | 报告声称 | 实际代码 | 一致 |
|------|----------|----------|------|
| `resolve.ts:20-22` | 新增 `conn: Tx \| typeof db = db` 可选参数 | `resolveEditingVersionId(agentId: string, conn: Tx \| typeof db = db)` | ✅ |
| `run/route.ts:61,147` | 配置查询 + run 创建包入 `db.transaction({ isolationLevel: "repeatable read" })` | `db.transaction(async (tx) => { ... }, { isolationLevel: "repeatable read" })` | ✅ |
| `batch/route.ts:68,180` | 配置查询 + batch/run 创建包入同一事务 | 同上模式，循环创建 N 个 run 全在 `tx` 内 | ✅ |

### 结果
✅ 充分
关键声明均有代码级证据支撑，代码抽查与报告描述一致。

## 4. Verdict Soundness（裁定合理性）

### 证据-结论一致性
四项检查全部通过 → Verdict 为"合并"。逻辑自然推出，无跳跃。

### 因果一致性特别审查
修复方式是**将查询包入数据库事务 + 指定 Repeatable Read 隔离级别**——这是利用数据库原生机制解决并发读一致性问题，属于**治病**而非止痛。

值得注意的是，验证阶段主动发现了初始修复的不足（Read Committed 下事务内各 SELECT 仍有独立快照），引用 PostgreSQL 官方文档论证后将隔离级别升级为 Repeatable Read。这说明验证过程具有独立批判性，不是走过场。

### 遗漏风险
无。FIX_REPORT 和 VERIFY_REPORT 均诚实披露了两项残留事项：
1. 并发检查 TOCTOU（明确标注为独立缺陷）
2. `Tx` 类型位置（非阻塞性建议）

### 守护报告信号
TEST_GUARD_REPORT：19 个测试全部通过，覆盖 Cause Anchor（11）、Boundary Set（6）、Blast Shield（2），裁定"守护就绪"。与 VERIFY 裁定一致。

### 结果
✅ 合理
Verdict 从证据自然推出，验证过程展现了独立批判性（发现并修正隔离级别遗漏）。

## 5. Decision（合并决策）

### 判决
✅ 可合并

### 四维度摘要
- **Completeness**：四份报告齐全，核心要素无缺失
- **Consistency**：因果链 Delta→Root Cause→Change→Reproduction 完整闭合，三份报告无矛盾
- **Evidence**：代码审计证据充分，抽查与报告声明一致，Repeatable Read 的必要性有 PostgreSQL 文档佐证
- **Soundness**：四项检查全通过推出合并结论合理，验证阶段独立发现并修正了隔离级别遗漏

### 理由
证据链完整且一致。修复使用数据库原生事务隔离机制（Repeatable Read），是针对根因的正确解法。验证过程有独立批判性——发现初始修复遗漏隔离级别并推动修正，增强了裁定可信度。守护测试 19 用例全过，回归无新增失败。

### 阻塞项
无

### 建议
1. 后续可考虑将 `Tx` 类型从 `resolve.ts` 提取到 `@/db/types`，方便其他模块复用
2. `existingRunningBatch` / `existingRunning` 的 TOCTOU 竞态可作为独立缺陷跟踪
