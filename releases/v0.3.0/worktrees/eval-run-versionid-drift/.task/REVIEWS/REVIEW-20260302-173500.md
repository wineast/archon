# 评审意见书：Eval Run versionId 快照——证据链完整，可合并

> 评审时间：2026-03-02 17:35
> 链路类型：缺陷链路
> 审查报告：DEFECT.md, FIX_REPORT.md, VERIFY_REPORT.md, TEST_GUARD_REPORT.md
> 分支：`dev-eval-run-versionid-drift-20260302`

## 0. 审查范围

| 报告 | 角色 | 状态 |
|------|------|------|
| DEFECT.md | 起诉书 | ✅ 已读取 |
| FIX_REPORT.md | 辩护词 | ✅ 已读取 |
| VERIFY_REPORT.md | 审判书 | ✅ 已读取 |
| TEST_GUARD_REPORT.md | 守护测试 | ✅ 已读取 |

## 1. Chain Completeness（链路完整性）

### 报告存在性
四份报告齐全（含可选守护报告）。

### 骨架完整性
| 报告 | 缺失要素 |
|------|----------|
| DEFECT.md | — |
| FIX_REPORT.md | — |
| VERIFY_REPORT.md | — |

- **DEFECT.md**：Delta（§1 期望 vs 实际明确）✅、Reproduction Path（§2 三步操作 + 代码调用链）✅、Location（§3 六处代码定位）✅、Impact（§4 严重度 + 范围 + 描述）✅
- **FIX_REPORT.md**：Root Cause（§1 因果链三步）✅、Change（§2 八行改动明细表）✅、Rationale（§3 两个排除方案 + 选择理由）✅、Blast Radius（§4 直接/间接/不影响）✅、Verification（§5 静态检查 + 数据流验证）✅
- **VERIFY_REPORT.md**：Reproduction Result（§1 五步数据流 + Grep 确认）✅、Cause-Fix Coherence（§2 三项检查）✅、Boundary Validation（§3 四个变体）✅、Regression Result（§4 typecheck + 1381 用例）✅、Verdict（§5 判决 + 摘要 + 残留风险）✅

### 结果
✅ 完整
所有必要报告存在，核心要素齐全。

## 2. Cross-Report Consistency（跨报告一致性）

### DEFECT → FIX 追溯

| DEFECT 条目 | 类型 | FIX 对应 | 结果 |
|------------|------|----------|------|
| versionId 未快照，case 用实时 resolveEditingVersionId 导致漂移 | Delta→Root Cause | evalRuns 表遗漏 versionId，resolveEditingVersionId 查可变字段 | ✅ 因果通——Root Cause 精确解释了 Delta |
| 三步复现：发起 run → 切换版本 → 后续 case 用新版本资源 | Path→Verification | 数据流验证：route.ts 写入 chatVersionId → execute-case.ts 从 run 快照读取 | ✅ 镜像——修复后路径是原始路径的逆操作 |
| 6 处代码位置（schema/route/execute-case） | Location→Change | 修改 schema.ts + route.ts + execute-case.ts + batch/route.ts + 2 个测试文件 | ✅ 吻合——改动覆盖了所有定位点 |
| 所有 eval 用户，版本变更时必现 | Impact→Blast Radius | 直接影响 eval run/batch 创建 + executeCase，不影响 chat/发布/只读接口 | ✅ 覆盖——Blast Radius 覆盖了 Impact 区域 |

### FIX → VERIFY 追溯

| FIX 声明 | 类型 | VERIFY 对应 | 结果 |
|----------|------|-------------|------|
| Root Cause：resolveEditingVersionId 查可变字段 | Root Cause→Coherence | 三项检查：因果通 + 对症 + 无漏洞 | ✅ |
| Blast Radius：eval run/batch 创建 + executeCase 直接影响 | Blast Radius→Regression | 七个区域逐一验证（含 batch 补充修复） | ✅ |

### DEFECT → VERIFY 跨层追溯

| 检查项 | 结果 |
|--------|------|
| Reproduction 走的是原始 Path？ | ✅ Verify 验证了 route.ts→execute-case.ts 的完整数据流，与 DEFECT 的调用链一致 |
| Delta 的期望行为已恢复？ | ✅ 修复后所有 case 使用 run 创建时的 versionId 快照，符合 DEFECT §1 的期望行为 |

### 发现的偏差
无。

**补充说明**：VERIFY 在边界验证中发现 FIX 最初遗漏了 `batch/route.ts`，并要求修复——这属于验证过程中的正常补充修复（已反映在最终的 FIX_REPORT 修改明细表中），不构成跨报告矛盾。

### 结果
✅ 一致
链式传递无断裂，三份报告事实陈述吻合。

## 3. Evidence Sufficiency（证据充分性）

### 验证报告证据审查

| 维度 | 有结论 | 有证据 | 证据支撑结论 |
|------|--------|--------|-------------|
| Reproduction | ✅ | ✅ Grep 零匹配 + 数据流五步追溯 | ✅ |
| Coherence | ✅ | ✅ 逻辑推理（三项对照） | ✅ |
| Boundary | ✅ | ✅ 四个变体表格 | ✅ |
| Regression | ✅ | ✅ typecheck + 1381 用例 | ✅ |

**注**：VERIFY_REPORT.assets/ 目录为空——因为此缺陷是代码级竞态条件，验证方式为代码审查 + Grep + 测试运行，非 UI 截图验证。验证方式选择合理。

### 代码抽查
| 文件 | 报告声称 | 实际代码 | 一致 |
|------|----------|----------|------|
| `web/src/db/schema.ts:726` | 新增 `chatVersionId: uuid("chat_version_id")` nullable | 实际 L726: `chatVersionId: uuid("chat_version_id"),` 无 `.notNull()` | ✅ |
| `web/src/app/api/eval/run/route.ts:120` | 插入时写入 `chatVersionId: versionId` | 实际 L120: `chatVersionId: versionId,` | ✅ |
| `web/src/lib/eval/execute-case.ts:135` | `run.chatVersionId ?? undefined` 替代实时查询 | 实际 L135: `const evalVersionId = run.chatVersionId ?? undefined;`，文件中 `resolveEditingVersionId` 零匹配 | ✅ |
| `web/src/app/api/eval/batch/route.ts:149` | batch 路径也写入 chatVersionId | 实际 L149: `chatVersionId: versionId,` | ✅ |

### 结果
✅ 充分
关键声明均有代码级证据支撑，抽查四处全部一致。

## 4. Verdict Soundness（裁定合理性）

### 证据-结论一致性
Verify 四维度全部 ✅ → Verdict "合并"。推导成立。

### 因果一致性特别审查
修复方式是**快照 versionId 到 run 记录**——与已有的 modelConfig 快照模式（chatModel/chatSystemPrompt/chatTemperature）完全对称。这是从机理上消除竞态（切断对可变字段的实时依赖），而非止痛式修复。

无可疑模式：不是"加延迟"、不是"加 try-catch"、不是"加空值检查但不知 null 来源"。快照是正确的架构方案。

### 遗漏风险
FIX_REPORT 和 VERIFY_REPORT 均提到 Judge agent 的 versionId 未做同样快照——但 judge 的 modelConfig 和 judgeConfig 已作为 JSONB 全量快照，当前不依赖运行时 versionId 加载版本化资源。此为合理的已知局限，已建议记为 tech debt。

### 守护报告信号
TEST_GUARD_REPORT：9 个用例（3 Cause Anchor + 3 Boundary + 3 Blast Shield）全部通过。
- Cause Anchor 用源码字符串断言（`readFileSync` + `not.toContain`）防止回归引入实时查询——这是最强的防线
- 守护结果与 Verify Verdict 一致

### 结果
✅ 合理
Verdict 从证据自然推出，修复方案是对症的架构修复而非止痛。

## 5. Decision（合并决策）

### 判决
✅ 可合并

### 四维度摘要
- **Completeness**：四份报告齐全，核心要素无缺失
- **Consistency**：三份报告间链式追溯无断裂，事实陈述吻合
- **Evidence**：代码级证据充分，四处抽查全部一致
- **Soundness**：Verdict 推导合理，快照方案对症且与现有架构一致

### 理由
修复精准对症（快照 versionId 消除竞态），改动最小（3 业务文件 + 2 测试文件 + 1 守护测试文件），与现有 modelConfig 快照模式对称一致。代码抽查确认所有声明属实。守护测试 9/9 通过，源码级 Cause Anchor 提供了强回归防线。

### 阻塞项
无

### 建议
- Judge agent 未来若需加载版本化资源（如 tools/datasets），需补充同样的 versionId 快照——建议创建 tech debt issue 跟踪
