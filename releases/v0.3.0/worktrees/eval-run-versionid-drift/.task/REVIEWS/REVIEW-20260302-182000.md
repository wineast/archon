# 评审意见书：Eval Run versionId 快照修复——证据链完整，可合并

> 评审时间：2026-03-02 18:20
> 链路类型：缺陷链路
> 审查报告：DEFECT.md, FIX_REPORT.md, VERIFY_REPORT.md, TEST_GUARD_REPORT.md
> 分支：`dev-eval-run-versionid-drift-20260302`

## 0. 审查范围

| 报告 | 角色 | 状态 |
|------|------|------|
| DEFECT.md | 起诉书 | ✅ 已读取 |
| FIX_REPORT.md | 辩护词 | ✅ 已读取 |
| VERIFY_REPORT.md | 审判书 | ✅ 已读取 |
| TEST_GUARD_REPORT.md | 守护测试 | ✅ 已读取 |

## 1. Chain Completeness（链路完整性）

### 报告存在性
四份报告全部存在（含可选的守护报告）。

### 骨架完整性
| 报告 | 缺失要素 |
|------|----------|
| DEFECT.md | — |
| FIX_REPORT.md | — |
| VERIFY_REPORT.md | — |

- **DEFECT.md**：Delta（期望/实际/偏差）✅、Reproduction Path（步骤+代码证据）✅、Location（6 处代码定位+因果链图）✅、Impact（严重度+范围+描述）✅
- **FIX_REPORT.md**：Root Cause（因果链 3 步）✅、Change（8 文件明细表）✅、Rationale（2 个排除方案+已知局限）✅、Blast Radius（直接/间接/不影响三层）✅、Verification（静态+正向+回归）✅
- **VERIFY_REPORT.md**：Reproduction Result ✅、Cause-Fix Coherence（3 项子检查）✅、Boundary Validation（4 个变体）✅、Regression Result（静态+Blast Radius 逐项）✅、Verdict ✅

### 结果
✅ 完整。所有必需报告存在，核心要素齐全。

## 2. Cross-Report Consistency（跨报告一致性）

### DEFECT → FIX 追溯

| DEFECT 条目 | 类型 | FIX 对应 | 结果 |
|------------|------|----------|------|
| Delta：case 执行用实时 versionId，与快照 systemPrompt 不匹配 | Delta→Root Cause | evalRuns 未存 versionId，executeCase 实时查询 resolveEditingVersionId | ✅ 因果通——未存储导致实时查询，实时查询导致漂移 |
| Path：run 执行期间切换版本 → 后续 case 用新版本 | Path→Verification | 修复后数据流：route.ts 写入 chatVersionId → execute-case.ts 读快照值 | ✅ 镜像——修复后切换版本不影响已有 run |
| Location：schema.ts:717-747 缺列、route.ts:116-146 缺字段、execute-case.ts:135-137 实时查询 | Location→Change | schema.ts:726 新增列、route.ts:119 新增字段、execute-case.ts:136 改读快照 | ✅ 吻合——三处定位与三处修改一一对应 |
| Impact：所有 eval 用户、版本变更时必现 | Impact→Blast Radius | 直接影响 eval run 创建 + case 执行，不影响 chat/发布/只读接口 | ✅ 覆盖——Blast Radius 范围合理，不存在缩水 |

### FIX → VERIFY 追溯

| FIX 声明 | 类型 | VERIFY 对应 | 结果 |
|----------|------|-------------|------|
| Root Cause：resolveEditingVersionId 实时查询可变字段 | Root Cause→Coherence | 三项检查全通过：因果通顺、Change 消除根因、Rationale 无漏洞 | ✅ |
| Blast Radius：直接影响 run 创建 + case 执行 | Blast Radius→Regression | 7 个区域逐项验证通过（含验证中发现的 batch 遗漏已修复） | ✅ |
| Blast Radius：不影响 chat/发布/只读接口 | Blast Radius→Regression | 对应区域确认无关联 | ✅ |

### DEFECT → VERIFY 跨层追溯

| 检查项 | 结果 |
|--------|------|
| Reproduction 走的是原始 Path？ | ✅ 走的是代码审查路径——DEFECT 原始路径就是代码级竞态分析，VERIFY 用同一条代码路径验证（route.ts→execute-case.ts 数据流），一致 |
| Delta 的期望行为已恢复？ | ✅ 期望"所有 case 使用同一 versionId"→修复后 case 从 run.chatVersionId 读取快照值，不再实时查询 |

### 发现的偏差
无。

### 结果
✅ 一致。链式传递无断裂，三份报告之间的事实陈述高度吻合。

## 3. Evidence Sufficiency（证据充分性）

### 验证报告证据审查

| 维度 | 有结论 | 有证据 | 证据支撑结论 |
|------|--------|--------|-------------|
| Reproduction | ✅ | ✅（代码行级审查 5 步数据流） | ✅ |
| Coherence | ✅ | ✅（3 项子检查逐条论证） | ✅ |
| Boundary | ✅ | ✅（4 个变体表格，含 batch 路径遗漏发现） | ✅ |
| Regression | ✅ | ✅（typecheck + 1381 测试 + 7 区域逐项表格） | ✅ |

注：无截图证据（`.task/VERIFY_REPORT.assets/` 不存在），但此缺陷为代码级竞态，不适用 UI 截图，代码审查证据充分。

### 代码抽查

| 文件 | 报告声称 | 实际代码 | 一致 |
|------|----------|----------|------|
| `web/src/db/schema.ts:726` | 新增 `chatVersionId: uuid("chat_version_id")` nullable | 实际代码 `chatVersionId: uuid("chat_version_id")`，无 `.notNull()`，确为 nullable | ✅ |
| `web/src/app/api/eval/run/route.ts:120` | 插入时写入 `chatVersionId: versionId` | 实际代码第 120 行 `chatVersionId: versionId` | ✅ |
| `web/src/lib/eval/execute-case.ts:135` | 读取 `run.chatVersionId ?? undefined` 替代 resolveEditingVersionId | 实际代码第 135 行 `const evalVersionId = run.chatVersionId ?? undefined;`，import 区无 resolveEditingVersionId | ✅ |
| `web/src/app/api/eval/batch/route.ts:149` | batch 路径也写入 chatVersionId | Grep 确认第 149 行 `chatVersionId: versionId` | ✅ |

### 结果
✅ 充分。所有关键声明都有代码级证据支撑，抽查 4 处均与报告描述一致。

## 4. Verdict Soundness（裁定合理性）

### 证据-结论一致性
四项检查全部通过（Reproduction ✅、Coherence ✅、Boundary ✅、Regression ✅）→ 裁定合并。这是最合理的推导路径。

### 因果一致性特别审查
修复方式是**快照 versionId 到 run 记录**，case 执行时读快照而非实时查询。这是从机理上消除竞态条件（切断对可变字段的依赖），而非止痛式修复。没有发现以下可疑模式：
- ❌ 加延迟解决竞态
- ❌ 加 try-catch 吞异常
- ❌ 加空值检查但不知 null 来源

`?? undefined` 处理的是旧数据兼容（chatVersionId = null 的历史 run），有明确来源和安全降级路径，属于合理的防御性编程。

### 遗漏风险
FIX_REPORT 已声明的已知局限：Judge agent 的 versionId 未做同样快照。VERIFY_REPORT 在 Boundary 中确认 judge 配置已作为 JSONB 全量快照，当前无风险。此为合理的 tech debt 标记，不构成阻塞。

### 守护报告信号
TEST_GUARD_REPORT：9/9 测试全部通过，覆盖 Cause Anchor（3）、Boundary（3）、Blast Shield（3）。特别是 Cause Anchor #1 使用源码字符串断言（`readFileSync` + `not.toContain("resolveEditingVersionId")`），直接防止根因重引入。与 Verdict 一致。

### 结果
✅ 合理。裁定从证据自然推出，无逻辑跳跃。

## 5. Decision（合并决策）

### 判决
✅ 可合并

### 四维度摘要
- **Completeness**：四份报告齐全，核心要素无缺失
- **Consistency**：三份报告间链式传递无断裂，DEFECT→FIX→VERIFY 事实陈述高度吻合
- **Evidence**：关键声明均有代码级证据，代码抽查 4 处全部一致
- **Soundness**：四维验证全通过→合并，推导合理；修复从机理消除竞态，非止痛；守护 9/9 通过

### 理由
证据链完整且一致。缺陷定位准确（evalRuns 未存 versionId + executeCase 实时查询），修复对症（快照 versionId 到 run 记录），验证覆盖充分（含 batch 路径遗漏发现与修复），守护测试到位（源码级 Cause Anchor 防回归）。四个维度均无阻塞性问题。

### 阻塞项
无

### 建议
- Judge agent versionId 快照已标记为 tech debt，建议在 judge 需要加载版本化资源时处理
