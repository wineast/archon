---
priority: P2
---
# 在 Parameters 预览中支持展开 object 类型子字段

Tool 详情页的 Parameters 预览 tab，当参数类型是 object 或 object[] 时只显示一个类型标签，不展示内部子字段。应支持展开查看子属性（如 name、gender、nickName 及其类型、required、description）。

> Anchor: `web/src/app/[locale]/(app)/[orgSlug]/agents/[agentId]/build/tools/`
